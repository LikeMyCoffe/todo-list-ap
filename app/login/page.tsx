'use client';

import Auth from '../../components/Auth';

/**
 * LoginPage component renders the authentication UI for user login.
 * Uses the Auth component to handle authentication logic and UI.
 */
export default function LoginPage() {
  // Render the Auth component for user login
  return <Auth />;
}
